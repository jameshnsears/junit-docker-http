work on DockerClientTest

check precondition used everywhere!

do more pretty printing:
        Gson gsonPrettyPrinter = new GsonBuilder().setPrettyPrinting().create();
        return gsonPrettyPrinter.toJson(containerCreateRequest);

a test to create the same network twice

use coverage + other static analsys tools to rm as much as possible - especially in Models
= +incoproate more pre-condition tests

work on JunitDockerHttpExtensionTest to actually use the extension

check volumes + networks created correctly in pytest-docker-py

in configurationaccessor use more Models!?

update pom.xml in all projects!

alt + enter = exception

--

https://pmd.github.io/
https://stackoverflow.com/questions/52666284/pmd-options-not-visible-in-spring-tool-suite-3-9-6
    https://dl.bintray.com/pmd/pmd-eclipse-plugin/snapshots/updates/

--

mvn clean test spotbugs:check checkstyle:check jacoco:report pmd:cpd-check pmd:pmd-check

mvn clean jacoco:report checkstyle:checkstyle pmd:check
= target/site/jacoco/index.html
= target/site/checkstyle.html
= target/site/pmd.html

--

cd /var/run

sudo mv /var/run/docker.sock /var/run/hidden.socket

sudo socat -t100 -x -v UNIX-LISTEN:/var/run/docker.sock,mode=777,reuseaddr,fork UNIX-CONNECT:/var/run/hidden.socket

sudo mv /var/run/hidden.socket /var/run/docker.sock

--

