language: java

sudo: required

notifications:
email: james.hn.sears@gmail.com

services:
  - docker

jdk:
  - oraclejdk11

cache:
  directories:
  - $HOME/.m2

script:
  - mvn clean
  - mvn test
  - mvn jacoco:report coveralls:report
  - mvn checkstyle:check
  - mvn spotbugs:check
